menu "RISC-V 64 configs"

config RISCV_S_MODE
    bool "RT-Thread run in RISC-V S-Mode(supervisor mode)"
    default y

config RISCV_DEFINE_PLIC_OFFSET
    bool "define hartid to plic num mapping function"
    default y

endmenu
